spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/router_watchdog
    username: watchdog
    password: watchdog

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080